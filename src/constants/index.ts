const BASE_URL = "https://api.escuelajs.co";
const PREFIX = "/api";
const VERSION = "/v1";

const ERROR_MESSAGES = {
  TIME_OUT: "Time out!",
};

const URL_PATHS = {
  LOGIN: "/auth/login/",
  PROFILE: "/auth/profile/",
};

const COOKIES = {
  TOKEN: "token",
};

export { BASE_URL, ERROR_MESSAGES, PREFIX, VERSION, URL_PATHS, COOKIES };
